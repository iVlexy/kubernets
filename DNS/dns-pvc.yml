apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: technitium-dns-pvc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi # Adjust storage size as needed

